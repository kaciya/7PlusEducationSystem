// 设置基准地址 baseUrl
// 未来要可以配置
// 只要是导出的配置对象就可以
export const baseHost = {
  API1: {
    // "/api" 在config中配置 相当于 http://pte.admin.api.banyuge.com/admin
    dev_host: "/api",
    // 线上的基准地址
    pro_host: "/api"
  }
};
